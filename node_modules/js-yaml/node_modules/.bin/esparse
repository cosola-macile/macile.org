#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../svg-sprite/node_modules/svgo/node_modules/esprima/bin/esparse.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../svg-sprite/node_modules/svgo/node_modules/esprima/bin/esparse.js" "$@"
  ret=$?
fi
exit $ret
